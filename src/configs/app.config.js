const appConfig = {
    apiPrefix: '/api',
    authenticatedEntryPath: '/dashboard',
    unAuthenticatedEntryPath: '/auth/login',
    tourPath: '/',
    locale: 'en',
    enableMock: true,
}

export default appConfig 